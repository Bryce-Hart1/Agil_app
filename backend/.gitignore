# Claude  Date 06/08/2026
# Rust build artifacts and local-only files.
/target
# SQLite database files (local data — not source)
gym.db
gym.db-shm
gym.db-wal
# Local environment / secrets
.env
# Machine-specific cargo target-dir override (absolute local path; would break
# builds on other machines / the server). Local convenience only.
/.cargo/config.toml
