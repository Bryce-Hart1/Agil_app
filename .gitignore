# Xcode / build artifacts
build/
DerivedData/
*.xcuserstate
xcuserdata/
#local run
run.sh
.gitignore
# Generated by XcodeGen — recreate with `xcodegen generate`.
# NOTE: project.yml is the SOURCE for that generation and must stay tracked —
# the .xcodeproj below is ignored, so without project.yml there's no buildable
# project. (Do not re-add project.yml here.)
GymApp.xcodeproj/
project.pbxproj
agil.xcodeproj

#local md files for ideas and claude
.claude
new_ideas.md
claude_notes.md
CLAUDE.md
backend_card_contract.md
#for looking over current design
polish.md

# macOS
.DS_Store

#Backend internals
#just showing src
.cargo
.vscode
Cargo.lock
#local running (mac)
Info.plist
GymApp 2.xcodeproj
GymApp 3.xcodeproj

#assets (not public)
assets

#for future, new workouts 
new_workouts
